import Nerv from "nervjs";
import Taro from "@tarojs/taro-h5";
import { View } from '@tarojs/components';
import styles from "./style/index";

class Spin extends Taro.Component {
  render() {
    const { type = 2 } = this.props;

    return <View className={styles[type === 1 ? 'loading_page' : 'loading_page2']}>
      <View className={styles.line_items} />
      <View className={styles.line_items} />
    </View>;
  }

}

export default Spin;