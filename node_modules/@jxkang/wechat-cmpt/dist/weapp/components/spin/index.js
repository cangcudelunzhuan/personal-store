import Taro from '@tarojs/taro';
import { View } from '@tarojs/components';
import styles from './style';

const Spin = ({type = 2}) => {
  return (
    <View className={styles[type===1?'loading_page':'loading_page2']}>
      <View className={styles.line_items} />
      <View className={styles.line_items} />
    </View>
  );
};

export default Spin;